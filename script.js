/*
 * AniFox 2.4 - Дополнительные скрипты
 * 
 * 💻 Разработано SerGio Play
 * 🌐 Веб-сайт: https://sergioplay-dev.vercel.app/
 * 📁 GitHub: https://github.com/SerGioPlay01/anifox-search
 * 
 * При использовании данного проекта обязательно указывайте ссылку на разработчика.
 * 
 * Функции:
 * - Управление кнопкой "Наверх"
 * - Обработка скролла страницы
 */

// ===========================================
// ОБРАБОТКА СКРОЛЛА
// ===========================================

// Обработчик события скролла страницы
window.onscroll = function() {scrollFunction()};

/**
 * Функция обработки скролла
 * Показывает/скрывает кнопку "Наверх" в зависимости от позиции скролла
 */
function scrollFunction() {
  // Проверяем, проскроллил ли пользователь больше 20px
  if (document.body.scrollTop > 20 || document.documentElement.scrollTop > 20) {
    // Показываем кнопку "Наверх"
    document.getElementById("scrollToTop").classList.add("show");
  } else {
    // Скрываем кнопку "Наверх"
    document.getElementById("scrollToTop").classList.remove("show");
  }
}

/**
 * Функция прокрутки наверх страницы
 * Плавно возвращает пользователя к началу страницы
 */
function scrollToTop() {
  // Прокручиваем к началу страницы
  document.body.scrollTop = 0;
  document.documentElement.scrollTop = 0;
}